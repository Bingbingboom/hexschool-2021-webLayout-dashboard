<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <title>
    <%- title %>
  </title>
  <!-- 字型 -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Lato:wght@400;700&display=swap" rel="stylesheet">
  <!-- Fontawesome -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.13.0/css/all.min.css" />
  <!-- Material icons -->
  <link href="https://fonts.googleapis.com/icon?family=Material+Icons"
      rel="stylesheet">
  <link rel="stylesheet" href="./assets/style/all.css">
</head>

<body>
  <div class="container-fluid">
    <div class="row">
      <div class="sidebar col-3 d-flex flex-column justify-content-between bg-light g-0">
        <div>
          <h1 class="p-9"><a href="index.html" class="logo">ANSWER</a></h1>
          <ul class="menu p-0 list-unstyled">
            <!-- Dashboard Btn -->
            <% if(pages=="dashboard") { %>
              <li class="py-1 mb-7 active">
                <a href="index.html" class="ps-9 fw-bold d-flex align-items-center active">
                  <span class="material-icons me-4">
                    poll
                  </span>
                  Dashboard
                </a>
              </li>
            <% } else { %>
              <li class="py-1 mb-7">
                <a href="index.html" class="ps-9 fw-bold d-flex align-items-center">
                  <span class="material-icons me-4">
                    poll
                  </span>
                  Dashboard
                </a>
              </li>
            <% } %>
            <!-- Question Btn -->
            <% if(pages=="question") { %>
              <li class="py-1 mb-7 active">
                <a href="question.html" class="ps-9 fw-bold d-flex align-items-center active">
                  <span class="material-icons me-4">
                    question_answer
                    </span>
                  Question
                </a>
              </li>
            <% } else { %>
              <li class="py-1 mb-7">
                <a href="question.html" class="ps-9 fw-bold d-flex align-items-center">
                  <span class="material-icons me-4">
                    question_answer
                    </span>
                  Question
                </a>
              </li>
            <% } %> 
            <!-- Assignment Btn -->
            <% if(pages=="assignment") { %>
              <li class="py-1 mb-7 active">
                <a href="assignment.html" class="ps-9 fw-bold d-flex align-items-center active">
                  <span class="material-icons me-4">
                    assignment
                  </span>
                  Assignment
                </a>
              </li>
            <% } else { %>
              <li class="py-1 mb-7">
                <a href="assignment.html" class="ps-9 fw-bold d-flex align-items-center">
                  <span class="material-icons me-4">
                    assignment
                  </span>
                  Assignment
                </a>
              </li>
            <% } %>
            <!-- Course Btn -->
            <% if(pages=="course") { %>
              <li class="py-1 mb-7 active">
                <a href="course.html" class="ps-9 fw-bold d-flex align-items-center active">
                  <span class="material-icons me-4">
                    video_library
                  </span>
                  Course
                </a>
              </li>
            <% } else { %>
              <li class="py-1 mb-7">
                <a href="course.html" class="ps-9 fw-bold d-flex align-items-center">
                  <span class="material-icons me-4">
                    video_library
                  </span>
                  Course
                </a>
              </li>
            <% } %>
            <!-- Admin Btn -->
            <% if(pages=="admin") { %>
              <li class="py-1 mb-7 active">
                <a href="admin.html" class="ps-9 fw-bold d-flex align-items-center active">
                  <span class="material-icons me-4">
                    people
                  </span>
                  Admin
                </a>
              </li>
            <% } else { %>
              <li class="py-1 mb-7">
                <a href="admin.html" class="ps-9 fw-bold d-flex align-items-center">
                  <span class="material-icons me-4">
                    people
                  </span>
                  Admin
                </a>
              </li>
            <% } %>
            <!-- Settings Btn -->
            <% if(pages=="settings") { %>
              <li class="py-1 active">
                <a href="settings.html" class="ps-9 fw-bold d-flex align-items-center active">
                  <span class="material-icons me-4">
                    settings
                  </span>
                  Settings
                </a>
              </li>
            <% } else { %>
              <li class="py-1">
                <a href="settings.html" class="ps-9 fw-bold d-flex align-items-center">
                  <span class="material-icons me-4">
                    settings
                  </span>
                  Settings
                </a>
              </li>
            <% } %>
          </ul>
        </div>
        <!-- 使用者 -->
        <div class="account d-flex align-items-center pb-9 ps-9">
            <div class="account-img me-3">
              <img src="assets/images/andychen.jpeg" alt="">
            </div>
            <div class="account-info">
              <h3 class="h6 mb-0 fw-bold">Andy Chen</h3>
              <a href="mailto:" class="fs-7 link-secondary">andy0401@mail.com</a>
            </div>
        </div>
      </div>
      <main class="col-9 g-0">
        <%- contents %>
      </main>
    </div>
  </div>

    <script src="./assets/js/jquery-3.5.1.min.js"></script>
    <script src="./assets/js/vendors.js"></script>
    <script src="./assets/js/all.js"></script>
</body>

</html>